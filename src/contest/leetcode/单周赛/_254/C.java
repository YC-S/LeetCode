package contest.leetcode.单周赛._254;

public class C {
  public int minNonZeroProduct(int p) {
    // p
    // nums: [1, 2P] binary
    // x, y from nums
    // a bit in x swap with its corresponding bit in y
    // minimum non-zero product of nums
    if (p == 1) return p;
    else if (p == 1) return 6;

    // p = 2, nums = [01, 10, 11]
    return 1;
  }
}
